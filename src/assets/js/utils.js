import config from"./utils/config.js";import database from"./utils/database.js";import logger from"./utils/logger.js";import slider from"./utils/slider.js";export{config,database,logger,changePanel,addAccount,slider as Slider,accountSelect};function changePanel(id){let panel=document.querySelector(`.${id}`),active=document.querySelector(".active");active&&active.classList.toggle("active"),panel.classList.add("active")}function addAccount(data){let div=document.createElement("div");div.classList.add("account"),div.id=data.uuid,div.innerHTML=`\n        <img class="account-image" src="https://minotar.net/helm/${data.name}/100">\n        <div class="account-name">${data.name}</div>\n        <div class="account-uuid">${data.uuid}</div>\n        <div class="account-delete"><div class="icon-account-delete icon-account-delete-btn"></div></div>\n    `,document.querySelector(".accounts").appendChild(div)}function accountSelect(uuid){let account=document.getElementById(uuid),pseudo=account.querySelector(".account-name").innerText,activeAccount=document.querySelector(".active-account");activeAccount&&activeAccount.classList.toggle("active-account"),account.classList.add("active-account"),headplayer(pseudo)}function headplayer(pseudo){document.querySelector(".player-head").style.backgroundImage=`url(https://minotar.net/helm/${pseudo}/100)`}
